# FastWan-QAD blog assets

Assets copied in (✅ done):

- `latency_graph.png` — from `fastwan_qad_benchmark_linear.png` (also the hidden cover image).
- `videos/turbodiffusion/{0..9}.gif`
- `videos/lightx2v/{0..9}.gif`
- `videos/fastwan_qad_fp4/{0..9}.gif` — Ours (FP4 attn + FP4 linear), from `*_fp4_lin_fp4_attn.gif`
- `videos/fastwan_qad_fp8/{0..9}.gif` — Ours (FP8 attn + FP4 linear), from `*_fp4_lin_fp8_attn.gif`

Source prompt indices used (from `~/Downloads/final_final`): `1,2,3,4,5,6,7,8,10,11` → renamed `0..9`.

Still TODO before publishing:

- Fill placeholder links (`(#)`) in `index.md`: the three HuggingFace checkpoints and TAEHV.
- Flip `draft = true` → `false` in `index.md`.
- NOTE: these GIFs are full-resolution and total ~633 MB. Consider optimizing/downscaling before committing.
